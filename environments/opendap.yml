name: opendap
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - xarray=2024.1.0
  - dask
  - matplotlib
  - cartopy
  - h5netcdf
  - pip
  - pip:
      - netcdf4==1.6.2
      - pydap==3.5
      - notebook
      - ipywidgets
      - widgetsnbextension
      - pandas-profiling
      - lxml